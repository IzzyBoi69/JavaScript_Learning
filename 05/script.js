// Logical Operators
// AND --> All conditions must be true.
// OR --> either one or more condition should be true
// NOT --> converts true to false and false to true

const age = 20;
const gender = 'male';

if (age >= 18 && gender == "male") {
    console.log(true)
} else {
    console.log(false)
}